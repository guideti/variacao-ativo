import 'package:flutter/material.dart';

class StockHistoricalWidget extends StatelessWidget {
  const StockHistoricalWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('HISTORICAL'),
    );
  }
}
